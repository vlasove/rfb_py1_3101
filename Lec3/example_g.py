# Строки

first_name = "Alex" # input()
last_name = "Johnson" # input()

# Тип string
print("First_Name type is:", type(first_name))

# Строки можно конкатенировать
concat_result = first_name + " " + last_name + "!!!!"
print(concat_result)

# Мультипликативная конкатенация 
multiplicat_concat = first_name * 2 # a*3 = a + a + a
print(multiplicat_concat)

# Взятие длины
print("Количество символов в строке:", len("#"))

# В Python нет типа char