# Сокращенные декременты/инкерменты
count = 0

while True:
    num = int(input())
    if num < 0:
        break

    count += 1 # count = count + 1
    # count -= 1 # count = count - 1
    # count *= 2 # count = count * 2
    # count /= 3 # count = count / 3
    # count %= 2 # count = count % 2
    # count **= 3 # count = count ** 3
    # count //= 10 # count = count // 10